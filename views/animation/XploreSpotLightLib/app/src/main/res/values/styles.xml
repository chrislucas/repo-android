<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="PopupOverlay" parent="Theme.AppCompat.Light.DarkActionBar" />

    <style name="AppBarOverlay" parent="Theme.AppCompat.Light.DarkActionBar" />

    <style name="nav_theme" parent="AppTheme">
        <item name="android:textSize">16sp</item>

        <!--
        <item name="android:listPreferredItemHeight">30dp</item>
        <item name="android:listPreferredItemHeightLarge">30dp</item>
        <item name="android:listPreferredItemPaddingStart">60sp</item>
        -->
        <item name="android:listPreferredItemHeightSmall">55dp</item>
    </style>

</resources>
